apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: review-service-npool-top-ingress
  namespace: kube-system
  annotations:
    k8s.kuboard.cn/displayName: review-service-npool
    k8s.kuboard.cn/workload: review-service-npool
    kubernetes.io/ingress.class: "traefik-internet"
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: kube-system-strip-review-service-prefix@kubernetescrd,kube-system-common-headers@kubernetescrd

spec:
  rules:
    - host: api.npool.top
      http: &http_rules
        paths:
          - path: /api/review-service
            backend:
              serviceName: review-service
              servicePort: 50050
    - host: api.xpool.top
      http: *http_rules
    - host: www.npool.top
      http: *http_rules
    - host: www.xpool.top
      http: *http_rules

---

apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: strip-review-service-prefix
  namespace: kube-system
spec:
  stripPrefix:
    prefixes:
      - /api/review-service
